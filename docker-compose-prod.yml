# =============================================================================
# GdeiAssistant 生产环境全栈
# 单一配置入口：项目根目录 .env
# 使用：docker compose -f docker-compose-prod.yml up -d
# =============================================================================

networks:
  gdei-network:
    name: ${PROD_COMPOSE_NETWORK_NAME:-gdei-network}
    driver: bridge

services:
  mysql:
    image: mysql:8.0
    container_name: gdei-mysql
    networks:
      - gdei-network
    environment:
      MYSQL_ROOT_PASSWORD: "${DB_PASSWORD:?DB_PASSWORD is required for production}"
      MYSQL_CHARSET: utf8mb4
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    volumes:
      - mysql-data:/var/lib/mysql
      - ./db-init/mysql:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-p${DB_PASSWORD:?DB_PASSWORD is required for production}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s
    restart: unless-stopped

  redis:
    image: redis:alpine
    container_name: gdei-redis
    networks:
      - gdei-network
    env_file:
      - .env
    command:
      - sh
      - -c
      - |
        if [ -n "$$REDIS_PASSWORD" ]; then
          exec redis-server --appendonly yes --requirepass "$$REDIS_PASSWORD";
        else
          exec redis-server --appendonly yes;
        fi
    volumes:
      - redis-data:/data
    healthcheck:
      test:
        - CMD-SHELL
        - |
          if [ -n "$$REDIS_PASSWORD" ]; then
            redis-cli -a "$$REDIS_PASSWORD" ping;
          else
            redis-cli ping;
          fi
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  mongodb:
    image: mongo:8.0
    container_name: gdei-mongo
    networks:
      - gdei-network
    volumes:
      - mongo-data:/data/db
      - ./db-init/mongodb:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    restart: unless-stopped

  gdeiassistant-backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    image: gdeiassistant-backend:latest
    container_name: gdeiassistant-backend
    networks:
      - gdei-network
    depends_on:
      mysql: { condition: service_healthy }
      redis: { condition: service_healthy }
      mongodb: { condition: service_healthy }
    env_file:
      - .env
    environment:
      SPRING_PROFILES_ACTIVE: production
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: "${DB_NAME:-gdeiassistant}"
      DB_NAME_LOG: "${DB_NAME_LOG:-gdeiassistant_log}"
      DB_NAME_DATA: "${DB_NAME_DATA:-gdeiassistant_data}"
      DB_PASSWORD: "${DB_PASSWORD:?DB_PASSWORD is required for production}"
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: "${REDIS_PASSWORD:-}"
      REDIS_DATABASE: "${REDIS_DATABASE:-0}"
      MONGO_HOST: mongodb
      MONGO_PORT: 27017
      MONGO_DATABASE: "${MONGO_DATABASE:-gdeiassistant}"
      SERVER_PORT: 8080
      JWT_SECRET: "${JWT_SECRET:?JWT_SECRET is required for production}"
      CRON_SECRET: "${CRON_SECRET:?CRON_SECRET is required for production}"
      ENCRYPT_ENABLE: "true"
      ENCRYPT_PRIVATE_KEY: "${ENCRYPT_PRIVATE_KEY:?ENCRYPT_PRIVATE_KEY is required for production}"
    restart: unless-stopped

  gdeiassistant-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_APP_BASE_API: "${VITE_APP_BASE_API:-/api}"
        VITE_PE_EXTERNAL_URL: "${VITE_PE_EXTERNAL_URL:-}"
    image: gdeiassistant-frontend:latest
    container_name: gdeiassistant-frontend
    networks:
      - gdei-network
    depends_on:
      - gdeiassistant-backend
    ports:
      - "${FRONTEND_PROD_EXPOSE_PORT:-80}:80"
    restart: unless-stopped

volumes:
  mysql-data:
  redis-data:
  mongo-data:
