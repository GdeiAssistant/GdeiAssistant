# 合规落地 TODO

## 1. 校园账号凭证单独同意记录持久化

- 当前状态：已实现。后端新增 `campus_credential_consent` 表，并在校园账号密码登录显式同意后记录 `consentType`、`policyDate`、`effectiveDate`、`scene`、`consentedAt`、`revokedAt`、`revokedReason` 等元数据。
- 风险说明：存量部署需要手动执行升级 SQL，旧用户不会被回填历史同意记录，未重新授权前默认视为未开启快速认证。
- 建议方案：部署时同步执行 `db-init/mysql/upgrade-2026-05-11-campus-credential-controls.sql`，并在后续版本继续补充审计查询或后台留痕展示能力。
- 优先级：高
- 是否需要后端：是
- 是否需要运营人工处理：否

## 2. 校园凭证删除 / 撤回授权接口

- 当前状态：已实现。后端新增状态查询、授权撤回、校园凭证删除接口；撤回/删除会停用快速认证、清理 MySQL 中保存的校园凭证、清理已知 Redis 登录/会话凭证，并返回最新安全状态。
- 风险说明：存量部署若未执行升级 SQL，将无法使用新状态表与快速认证开关；更广义的个人信息删除流程仍未纳入本次实现范围。
- 建议方案：上线前确认数据库已升级，并在后续 PR 中继续补充广义个人信息删除工单与后台处理能力。
- 优先级：高
- 是否需要后端：是
- 是否需要运营人工处理：否

## 3. 快速认证关闭开关

- 当前状态：已实现。账号说明页已接入真实状态接口，可关闭或重新开启快速认证；开启前要求同时满足“存在有效授权记录 + 存在已保存校园凭证 + `is_quick_auth_allow=true`”。
- 风险说明：旧用户不会因为历史凭证自动获得快速认证资格，重新启用前需要重新授权并重新保存校园凭证。
- 建议方案：继续保留前端单独确认与后端校验双重约束，避免无授权的历史凭证继续被自动复用。
- 优先级：高
- 是否需要后端：是
- 是否需要运营人工处理：否

## 4. 敏感日志统一脱敏中间件

- 当前状态：本轮已收口前端高风险日志，并对后端关键请求/查询日志增加脱敏。
- 风险说明：仍需继续排查历史日志点和新增功能日志，避免后续回归。
- 建议方案：统一梳理日志规范，补充敏感字段脱敏工具和测试覆盖。
- 优先级：高
- 是否需要后端：是
- 是否需要运营人工处理：否

## 5. 第三方服务启用状态与隐私清单同步

- 当前状态：第三方服务清单以“可能启用 / 视配置而定”为主，依赖人工同步。
- 风险说明：部署配置变化后，文档和页面可能滞后。
- 建议方案：建立发布前检查清单，或通过构建/部署脚本生成实际启用服务摘要。
- 优先级：中
- 是否需要后端：否
- 是否需要运营人工处理：是

## 6. 用户个人信息导出 / 删除请求处理流程

- 当前状态：已有部分下载能力，但完整删除/导出流程与处理 SLA 仍需人工确认。
- 风险说明：用户请求可能缺少统一入口、留痕和处理闭环。
- 建议方案：补充工单流转说明、处理状态记录和必要的后台操作入口。
- 优先级：中
- 是否需要后端：是
- 是否需要运营人工处理：是

## 7. 政策版本历史展示

- 当前状态：本轮新增 `docs/policy-changelog.md` 记录版本摘要，前端暂无历史版本页面。
- 风险说明：用户在产品内查看历史版本的路径仍不够直观。
- 建议方案：后续评估是否增加前端历史版本入口或发布公告页。
- 优先级：中
- 是否需要后端：否
- 是否需要运营人工处理：否

## 8. 交易 / 跑腿 / 失物纠纷处理后台记录

- 当前状态：规则文案已补充，但后台留痕能力和处理工单流程仍需确认。
- 风险说明：投诉、申诉和证据保留可能依赖临时人工处理。
- 建议方案：增加举报单、处理结论、证据附件和账号处置记录能力。
- 优先级：中
- 是否需要后端：是
- 是否需要运营人工处理：是

## 9. 未成年人相关处理流程

- 当前状态：政策中已采取保守表述，产品侧尚无专门识别或处置流程。
- 风险说明：收到有效通知后，缺少标准化删除、限制处理或补充监护人同意的步骤。
- 建议方案：补充内部处理 SOP 和最小必要的留痕字段。
- 优先级：低
- 是否需要后端：否
- 是否需要运营人工处理：是

## 10. 安全漏洞私密报告渠道

- 当前状态：仓库已有 `SECURITY.md`，但仍以公开反馈渠道和后续公布方式为主。
- 风险说明：高风险漏洞可能因公开披露过早而扩大影响。
- 建议方案：后续补充私密安全报告渠道，并同步到仓库与站内帮助页面。
- 优先级：中
- 是否需要后端：否
- 是否需要运营人工处理：是

## 11. 充值请求客户端 HMAC 迁移

- 当前状态：已完成后端、Android、iOS 收口。后端不再将客户端提交的 `hmac` / `timestamp` 作为可信安全边界；Android 和 iOS 已移除客户端内置 HMAC 共享密钥；`REQUEST_VALIDATE_TOKEN` 已不再作为部署配置项使用。旧 `hmac` / `timestamp` 字段仅作为兼容输入保留并被忽略，充值仍依赖登录态、设备标识、二次密码验证、金额校验、限流和审计日志。
- 风险说明：若旧密钥曾公开或进入发布包，应视为已泄露；当前后端已不再依赖该密钥作为安全边界。
- 建议方案：兼容期后再移除旧 `hmac` / `timestamp` 字段；继续补全客户端幂等键复用、订单状态表和 UNKNOWN 补偿能力。
- 优先级：中
- 是否需要后端：是
- 是否需要运营人工处理：否

## 12. 充值请求 Redis 幂等键基础机制

- 当前状态：部分实现。后端 `POST /api/card/charge` 已支持可选 `Idempotency-Key` / `X-Idempotency-Key`，并使用 Redis 原子写入短期幂等状态，避免同一用户、同一接口、同一幂等键、同一 payload fingerprint 的重复请求再次触发学校/支付链路。未提交幂等键的旧客户端仍保持兼容。
- 风险说明：当前阶段不新增订单状态表，也不处理学校/支付系统已成功但服务端或客户端收到失败结果的 UNKNOWN 补偿闭环。
- 建议方案：后续分别推进客户端生成并复用 `Idempotency-Key`、充值订单状态表、UNKNOWN 状态补偿、兼容期后强制新客户端提交幂等键，以及必要的充值查询或后台处理工具。
- 优先级：高
- 是否需要后端：是
- 是否需要运营人工处理：视 UNKNOWN 补偿方案而定

最近更新：2026年4月27日
