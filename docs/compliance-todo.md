# 合规落地 TODO

本文只保留仍需要跟进的合规、发布和运营事项。旧发布快照里的未完成项已合并到本文；
不要在本文填写真实密钥、账号、服务凭证或内部地址。

## 仍未完成项总览

| 事项 | 当前状态 | 优先级 | 需要后端 | 需要运营/部署确认 |
| --- | --- | --- | --- | --- |
| 生产部署、数据库升级、Redis 和客户端兼容性确认 | 待人工确认 | 高 | 是 | 是 |
| 第三方服务实际启用清单、跨境处理和备案信息 | 待人工确认 | 中 | 否 | 是 |
| 用户个人信息导出 / 删除请求闭环 | 部分能力已有，流程未闭环 | 中 | 是 | 是 |
| 交易 / 跑腿 / 失物纠纷处理后台留痕 | 文案已补充，后台流程未闭环 | 中 | 是 | 是 |
| 充值订单 UNKNOWN 自动补偿、MANUAL_REVIEW 工具和学校侧流水确认 | 基础订单状态已实现，补偿与对账未完成 | 高 | 是 | 是 |
| 兼容期后移除旧 `hmac` / `timestamp` 字段并强制幂等键 | 旧字段已不再可信，仅兼容保留 | 中 | 是 | 否 |
| 支付 URL / cookie 服务端中转方案评估 | 待评估 | 中 | 是 | 视方案而定 |
| mock / demo 数据和敏感日志持续巡检 | 需要随功能持续检查 | 中 | 视变更而定 | 否 |
| 政策版本历史入口、未成年人处理 SOP、私密安全报告渠道 | Git 已保留历史，产品/流程入口待评估 | 低到中 | 视变更而定 | 是 |

## 发布和部署前人工确认

- 确认生产后端已经部署当前主干，并包含校园凭证控制、充值幂等和充值订单状态相关变更。
- 确认数据库升级 SQL 已执行，至少包含校园凭证控制表和 `charge_order` 相关变更。
- 确认 Redis 在生产环境可用，TTL、容量和故障处理策略适合充值幂等场景。
- 确认 Android、iOS、Web、WeChatApp 发布版本与已部署后端 API 兼容。
- 确认旧 Android / iOS 版本兼容策略，尤其是未提交 `Idempotency-Key` 的旧客户端。
- 确认发布包不包含 mock 环境、debug 配置、本地测试账号、签名密钥或其他敏感配置。
- 在受控环境验证充值链路，确认 `PAYMENT_SESSION_CREATED` 不被描述为最终到账。
- 确认用户协议、隐私政策、第三方服务清单、关于页和帮助反馈入口均可访问。
- 确认 ICP / 公安备案等展示信息与实际生产域名一致。
- 确认 `JWT_SECRET`、加密密钥、数据库密码、对象存储密钥、SMTP/SMS 凭证等通过部署密钥管理。
- 确认 SMTP、SMS、对象存储、AI/OCR、日志监控、云主机、数据库、Redis/cache、备份等服务的实际启用状态。
- 确认是否涉及跨境处理或境外服务商，并同步更新公开隐私说明。
- 确认安全漏洞、隐私请求和用户投诉的接收与处理渠道可用。

## 1. 校园账号凭证单独同意记录持久化

- 当前状态：已实现。后端新增 `campus_credential_consent` 表，并在校园账号密码登录显式同意后记录 `consentType`、`policyDate`、`effectiveDate`、`scene`、`consentedAt`、`revokedAt`、`revokedReason` 等元数据。
- 未完成项：存量部署需要手动执行升级 SQL；后续可补充审计查询或后台留痕展示能力。
- 优先级：高
- 是否需要后端：是
- 是否需要运营人工处理：否

## 2. 校园凭证删除 / 撤回授权接口

- 当前状态：已实现。后端新增状态查询、授权撤回、校园凭证删除接口；撤回/删除会停用快速认证、清理 MySQL 中保存的校园凭证、清理已知 Redis 登录/会话凭证，并返回最新安全状态。
- 未完成项：更广义的个人信息删除流程仍未纳入本次实现范围。
- 优先级：高
- 是否需要后端：是
- 是否需要运营人工处理：否

## 3. 快速认证关闭开关

- 当前状态：已实现。账号说明页已接入真实状态接口，可关闭或重新开启快速认证；开启前要求同时满足“存在有效授权记录 + 存在已保存校园凭证 + `is_quick_auth_allow=true`”。
- 未完成项：旧用户不会因为历史凭证自动获得快速认证资格，重新启用前需要重新授权并重新保存校园凭证。
- 优先级：高
- 是否需要后端：是
- 是否需要运营人工处理：否

## 4. 敏感日志统一脱敏中间件

- 当前状态：已收口前端高风险日志，并对后端关键请求/查询日志增加脱敏。
- 未完成项：继续排查历史日志点和新增功能日志，避免后续回归。
- 优先级：高
- 是否需要后端：是
- 是否需要运营人工处理：否

## 5. 第三方服务启用状态与隐私清单同步

- 当前状态：第三方服务清单以“可能启用 / 视配置而定”为主，依赖人工同步。
- 未完成项：建立发布前检查清单，或通过构建/部署脚本生成实际启用服务摘要。
- 优先级：中
- 是否需要后端：否
- 是否需要运营人工处理：是

## 6. 用户个人信息导出 / 删除请求处理流程

- 当前状态：已有部分下载能力，但完整删除/导出流程与处理 SLA 仍需人工确认。
- 未完成项：补充工单流转说明、处理状态记录和必要的后台操作入口。
- 优先级：中
- 是否需要后端：是
- 是否需要运营人工处理：是

## 7. 政策版本历史展示

- 当前状态：版本变更由 Git 历史记录，前端暂无历史版本页面。
- 未完成项：后续评估是否增加前端历史版本入口或发布公告页。
- 优先级：中
- 是否需要后端：否
- 是否需要运营人工处理：否

## 8. 交易 / 跑腿 / 失物纠纷处理后台记录

- 当前状态：规则文案已补充，但后台留痕能力和处理工单流程仍需确认。
- 未完成项：增加举报单、处理结论、证据附件和账号处置记录能力。
- 优先级：中
- 是否需要后端：是
- 是否需要运营人工处理：是

## 9. 未成年人相关处理流程

- 当前状态：政策中已采取保守表述，产品侧尚无专门识别或处置流程。
- 未完成项：补充内部处理 SOP 和最小必要的留痕字段。
- 优先级：低
- 是否需要后端：否
- 是否需要运营人工处理：是

## 10. 安全漏洞私密报告渠道

- 当前状态：仓库已有 `SECURITY.md`，但仍以公开反馈渠道和后续公布方式为主。
- 未完成项：后续补充私密安全报告渠道，并同步到仓库与站内帮助页面。
- 优先级：中
- 是否需要后端：否
- 是否需要运营人工处理：是

## 11. 充值请求客户端 HMAC 迁移

- 当前状态：已完成后端、Android、iOS 收口。后端不再将客户端提交的 `hmac` / `timestamp` 作为可信安全边界；Android 和 iOS 已移除客户端内置 HMAC 共享密钥；`REQUEST_VALIDATE_TOKEN` 已不再作为部署配置项使用。旧 `hmac` / `timestamp` 字段仅作为兼容输入保留并被忽略。
- 未完成项：兼容期后移除旧 `hmac` / `timestamp` 字段。
- 优先级：中
- 是否需要后端：是
- 是否需要运营人工处理：否

## 12. 充值请求 Redis 幂等键基础机制

- 当前状态：已实现。后端 `POST /api/card/charge` 已支持可选 `Idempotency-Key` / `X-Idempotency-Key`，并使用 Redis 原子写入短期幂等状态；Android 和 iOS 已开始提交幂等键。未提交幂等键的旧客户端仍保持兼容。
- 未完成项：兼容期后再评估是否强制新客户端提交幂等键，并继续补全充值查询和 UNKNOWN 补偿能力。
- 优先级：高
- 是否需要后端：是
- 是否需要运营人工处理：视 UNKNOWN 补偿方案而定

## 13. 充值订单状态表和状态流转

- 当前状态：基础实现。后端新增 `charge_order` 表记录 `CREATED`、`PROCESSING`、`PAYMENT_SESSION_CREATED`、`FAILED`、`UNKNOWN`、`MANUAL_REVIEW` 等状态；Android 和 iOS 已接入订单状态展示。`PAYMENT_SESSION_CREATED` 仅表示支付会话或跳转链接已生成，不代表校园卡余额最终到账。
- 未完成项：UNKNOWN 自动补偿、MANUAL_REVIEW 处理工具、学校侧订单号/流水号/到账状态确认，以及支付 URL / cookie 服务端中转方案仍需继续推进。
- 优先级：高
- 是否需要后端：是
- 是否需要运营人工处理：是

最近更新：2026年4月29日
