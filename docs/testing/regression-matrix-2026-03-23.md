# Regression Matrix -- Governance Fixes (2026-03-23)

> Generated by scanning the actual test tree under `src/test/`, `frontend/test/`, and `frontend/e2e/`.
> "Exists" means a concrete test file was found and its assertions were inspected.
> "Missing" means no test file or assertion targeting the described behaviour was found anywhere in the repository.

---

## 1. Security / Auth

| Fix area | Current test coverage | Missing test layer | Owner repo |
|---|---|---|---|
| **JWT unification** (Bearer-only, legacy `token` header ignored) | `JwtSessionIdFilterTest` -- 4 unit tests: Bearer parse, legacy header ignore, revoked session, invalid JWT. `AuthWebFlowIntegrationTest` -- 6 integration tests wiring filter + interceptor via MockMvc: whitelist bypass, no-token 401, valid Bearer 200, legacy header 401, revoked session 401, bad JWT 401. | No native-client integration test (iOS / Android sending Bearer instead of legacy header). No Playwright e2e test exercising token refresh flow. | backend |
| **Session revocation** (logout clears Redis, subsequent requests get 401) | Covered in `JwtSessionIdFilterTest.shouldNotInjectSessionIdWhenSessionDeletedFromRedis` and `AuthWebFlowIntegrationTest.shouldRejectProtectedRouteWhenSessionRevokedAfterLogout`. | No Redis integration test (current tests mock `UserCertificateService`). No multi-device concurrent revocation test. | backend |
| **Upload path traversal** (MIME whitelist, extension-MIME match) | `ObjectStorageControllerTest` -- 11 tests: allowed/disallowed MIME, extension mismatch, blank filename, case insensitive, no-extension. | No test for `../` in fileName parameter. No test for double-extension (e.g., `image.jpg.exe`). No E2E upload test from any client. | backend |
| **Lost-and-found ownership** (only owner can modify/delete) | `LostAndFoundServiceTest` -- 3 tests (upload, delete item, delete images). Mapper integration in `MapperIntegrationTest.lostAndFoundSelectByUsername`. | No ownership-check unit test (non-owner attempt to update/delete). No contract test for L&F controller endpoints. | backend |
| **Delivery / Dating sensitive data stripping** | `DeliveryContractTest.detailEndpointStripsSensitiveFieldsForThirdParty` -- verifies `number` and `phone` are absent for `detailType=1`. `DatingContractTest` -- verifies profile fields and hidden-profile 404. `DeliveryServiceTest` -- ownership and role-based type resolution. | No test for dating pick-request contact visibility toggling. No iOS/Android client test asserting stripped fields are not rendered. | backend |
| **Cron auth** (scheduled task endpoints require internal auth) | No tests found. | Unit test for cron endpoint auth gate. Integration test verifying external callers cannot trigger scheduled tasks. | backend |
| **Device binding** | No source code or tests found for device binding in the repository. | Full test suite needed once feature is implemented. | backend, iOS, Android |

---

## 2. i18n

| Fix area | Current test coverage | Missing test layer | Owner repo |
|---|---|---|---|
| **Backend translation filter** (Accept-Language routing) | `I18nTranslationFilterTest` -- 5 tests: zh-CN passthrough, absent header passthrough, non-whitelisted path passthrough, cache-hit replacement with enqueue for miss, locale normalisation (ja-JP, zh-Hant, fallback en). | No integration test with real `I18nTranslationService` (current tests mock it). No test for concurrent translation enqueue dedup. | backend |
| **Four-client locale switching** (iOS, Android, WeChat, Web) | No client-side locale tests found. Frontend Playwright specs (`appearance.spec.js`) test theme/dark mode but not locale. `router.test.js` tests auth routing only. | E2E test per client verifying Accept-Language header is sent correctly. Playwright test for web locale selector UI. iOS/Android unit tests for locale persistence. | iOS, Android, WeChat, frontend |
| **Font scaling** | `appearance.spec.js` -- 1 test: `font scale slider is present` (checks `input[type="range"]` visibility). | No test asserting that the slider value actually changes CSS `font-size`. No iOS/Android accessibility font-size test. | frontend, iOS, Android |
| **Hardcoded text replacement** | No tests found targeting hardcoded Chinese text scanning or string extraction. | Lint rule or snapshot test asserting no raw Chinese strings appear in UI components outside i18n bundles. | frontend, iOS, Android, WeChat |
| **Mock data localisation** | No tests found. | Test that mock/seed data used in dev/test environments respects the active locale. | backend, frontend |

---

## 3. Media Atomicity (6-module publish rollback)

| Module | Current test coverage | Missing test layer | Owner repo |
|---|---|---|---|
| **Topic** | `TopicContractTest` -- 2 contract tests (list, detail). `MapperIntegrationTest.topicSelectPage`. | No service-level test. No rollback test: if image upload fails after DB insert, verify row is deleted. No `@Transactional` rollback assertion. | backend |
| **Photograph** | `PhotographServiceTest` -- 7 tests (comment validation, like, R2 upload failure throws). `PhotographContractTest` exists (contract). | R2 failure test exists but does not assert DB row rollback. No test for partial multi-image upload (image 2 of 3 fails). | backend |
| **Secret** | `SecretServiceTest` -- 12 tests (comment, like, batch queries). `SecretContractTest` -- 3 contract tests. `MapperIntegrationTest.secretSelectPage`. | `uploadVoiceSecret` R2 failure test exists but no rollback assertion. No multi-step publish atomicity test. | backend |
| **Dating** | `DatingServiceTest` -- 12 tests (profile CRUD, ownership, R2 failure). `DatingContractTest` -- 3 contract tests. | R2 failure test exists but no rollback assertion. No test for publish with image then DB failure. | backend |
| **Marketplace** | `MarketplaceServiceTest` -- 8 tests (query, detail, ownership, R2 failure, delete). `MapperIntegrationTest.marketplaceSelectAvailableItems` + 3 mapper tests. | R2 failure test exists but no rollback assertion. No partial-upload atomicity test. | backend |
| **Lost and Found** | `LostAndFoundServiceTest` -- 3 tests (R2 failure, delete, image delete). `MapperIntegrationTest.lostAndFoundSelectByUsername`. | No publish-with-image rollback test. No contract test for L&F controller. | backend |

**Summary**: Every module has an R2-failure-throws test, but none verify that the database row is rolled back when the upload fails mid-publish. No `@Transactional` rollback integration tests exist for any of the six modules.

---

## 4. Anonymization / Privacy (account deletion cleanup)

| Table / Module | Current test coverage | Missing test layer | Owner repo |
|---|---|---|---|
| **Account deletion orchestration** (`AccountDeletionService.deleteAccount`) | No test file found for `AccountDeletionService`. | Unit test mocking all mappers to verify `anonymizeUsername` is called for each module. Integration test with H2 verifying `del_` prefix username propagation. Rollback test if one mapper fails mid-transaction. | backend |
| **Topic** (`topicMapper.anonymizeUsername`) | No direct test. | Mapper integration test verifying topic rows get `del_` username after deletion. | backend |
| **Secret** (`secretMapper.anonymizeUsername`) | No direct test. | Mapper integration test. | backend |
| **Photograph** (`photographMapper.anonymizeUsername`) | No direct test. | Mapper integration test. | backend |
| **Express** (`expressMapper.anonymizeByUsername`, `anonymizeCommentsByUsername`) | No direct test. | Mapper integration test verifying both express posts and comments are anonymised. | backend |
| **Dating** (`datingMapper.hideDatingProfilesByUsername`) | No direct test. | Mapper integration test. | backend |
| **Notification** (`interactionNotificationMapper.anonymizeActorUsername`) | Partially covered: `InteractionNotificationServiceTest` tests that `del_` prefix actors and deleted actors display as "已注销用户" at read time (5 tests). | No test for the write-time `anonymizeActorUsername` mapper call during account deletion. No test verifying the content-string replacement is idempotent. | backend |

**Summary**: The read-path anonymization in `InteractionNotificationService` is well-tested (5 cases including mixed actors). The write-path cleanup in `AccountDeletionService.deleteAccount` has zero test coverage -- none of the mapper `anonymize*` calls are tested.

---

## 5. Performance / Query Efficiency

| Fix area | Current test coverage | Missing test layer | Owner repo |
|---|---|---|---|
| **Secret batch queries** (comment counts, like counts, liked-boolean in one pass) | `SecretServiceTest` -- 7 batch-specific tests: `listPathReturnsCorrectCommentCountPerItem`, `listPathReturnsCorrectLikeCountPerItem`, `listPathReturnsCorrectLikedBoolean`, `profilePathReturnsCorrectDerivedFields`, `listPathUsesLightweightQueryNotEagerComments`, `profilePathUsesLightweightQueryNotEagerComments`. Verifies `selectSecretLight` is called instead of N+1 `selectSecret`. | No load/performance benchmark. No mapper integration test for the batch SQL itself (H2 `MapperIntegrationTest` uses `selectSecret`, not `selectSecretLight`). | backend |
| **Notification batch lookup** (batch `selectExistingUsernames` instead of per-actor query) | `InteractionNotificationServiceTest` -- 5 tests: verifies `selectExistingUsernames` is called once for non-del actors, never called for empty list or del-only actors. | No mapper integration test for `selectExistingUsernames` SQL. No performance benchmark. | backend |
| **iOS secret overfetch** | No iOS tests in this repository. | iOS unit test verifying page size parameter is sent correctly. iOS integration test asserting response count matches requested size. | iOS |
| **WeChat inbox lazy load** | No WeChat tests in this repository. | WeChat mini-program test verifying lazy-load trigger fires at scroll threshold. Test verifying only one page is fetched on initial render. | WeChat |
| **Photograph comment dedup** | No tests found for comment deduplication. | Backend unit test verifying duplicate comments from the same user within a time window are rejected. Contract test for the dedup response. | backend |

---

## Cross-cutting gaps

| Gap | Affected categories | Recommendation |
|---|---|---|
| No `@Transactional` rollback tests anywhere | Media Atomicity, Anonymization | Add Spring `@Transactional` integration tests using H2 + deliberate mapper failures to verify rollback semantics. |
| No `AccountDeletionService` test | Anonymization | Highest-priority gap. One failing anonymize call could leave PII in production. |
| No client-side tests for iOS, Android, or WeChat | Security, i18n, Performance | Each native repo needs unit + integration tests mirroring the backend contract tests. |
| No E2E locale test | i18n | Add Playwright spec that sets `Accept-Language: ja` and verifies translated strings in the response body. |
| `MapperIntegrationTest` does not cover batch queries | Performance | Extend H2 schema and seed data to test `selectSecretLight`, `selectSecretCommentCounts`, `selectExistingUsernames`. |
