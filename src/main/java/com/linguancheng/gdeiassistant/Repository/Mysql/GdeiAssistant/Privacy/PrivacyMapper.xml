<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.linguancheng.gdeiassistant.Repository.Mysql.GdeiAssistant.Privacy.PrivacyMapper">

    <!-- 开启PrivacyMapper namespace下的二级缓存 -->
    <cache/>

    <select id="selectPrivacy" parameterType="java.lang.String"
            resultType="com.linguancheng.gdeiassistant.Pojo.Entity.Privacy">
        select * from privacy where username=#{username}
    </select>

    <insert id="initPrivacy">
        insert into privacy (username,gender,genderOrientation,faculty,major,region,introduction,cache)
         values(#{username},true,true,true,true,true,true,true)
    </insert>

    <update id="resetPrivacy">
        update privacy set gender=1,genderOrientation=1,faculty=1,major=1,region=1
        ,introduction=1,cache=1 where username=#{username}
    </update>

    <update id="updateGender">
        update privacy
        <choose>
            <when test="gender">
                set gender='1'
            </when>
            <otherwise>
                set gender='0'
            </otherwise>
        </choose>
        where username=#{username}
    </update>

    <update id="updateGenderOrientation">
        update privacy
        <choose>
            <when test="genderOrientation">
                set genderOrientation='1'
            </when>
            <otherwise>
                set genderOrientation='0'
            </otherwise>
        </choose>
        where username=#{username}
    </update>

    <update id="updateFaculty">
        update privacy
        <choose>
            <when test="faculty">
                set faculty='1'
            </when>
            <otherwise>
                set faculty='0'
            </otherwise>
        </choose>
        where username=#{username}
    </update>

    <update id="updateMajor">
        update privacy
        <choose>
            <when test="major">
                set major='1'
            </when>
            <otherwise>
                set faculty='0'
            </otherwise>
        </choose>
        where username=#{username}
    </update>

    <update id="updateRegion">
        update privacy
        <choose>
            <when test="region">
                set region='1'
            </when>
            <otherwise>
                set region='0'
            </otherwise>
        </choose>
        where username=#{username}
    </update>

    <update id="updateIntroduction">
        update privacy
        <choose>
            <when test="introduction">
                set introduction='1'
            </when>
            <otherwise>
                set introduction='0'
            </otherwise>
        </choose>
        where username=#{username}
    </update>

    <update id="updateCache">
        update privacy
        <choose>
            <when test="cache">
                set cache='1'
            </when>
            <otherwise>
                set cache='0'
            </otherwise>
        </choose>
        where username=#{username}
    </update>

</mapper>