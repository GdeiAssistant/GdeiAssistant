<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 控制台标准输出流（Native Image 仅保留控制台，避免编译期文件描述符泄漏） -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- RollingFileAppender 已移除，避免 GraalVM Native 编译期 Logback 打开文件导致 FD 泄漏；仅保留控制台。JVM 运行时如需文件日志可恢复。 -->

    <root level="info">
        <appender-ref ref="STDOUT"/>
    </root>

    <logger name="cn.gdeiassistant.common.aspect.QueryLogAspect" level="info" additivity="false">
        <appender-ref ref="STDOUT"/>
    </logger>

    <logger name="cn.gdeiassistant.common.aspect.RequestLogAspect" level="info" additivity="false">
        <appender-ref ref="STDOUT"/>
    </logger>

</configuration>
